# 面试官小P · 小白入门教程

> 这是一份写给完全没接触过编程/命令行的小白的使用教程。照着做，30 分钟以内就能跑起来。

## 这是什么？

**面试官小P** 是一个"AI 面试陪练"：你用文字或语音跟它对话，它扮演面试官，
按真实面试流程给你出题、点评、追问，最后给你一份评分报告。

它有两块界面：

- **文字版**（网页聊天）：模拟面试、辅导答疑、定制面试、题库浏览
- **语音通话**（像打电话一样）：接通后开口说话，小P 用语音回答，你随时可以打断

---

## 一、你需要准备什么

| 东西 | 说明 |
|------|------|
| 一台电脑 | Windows / macOS / Linux 都行 |
| Python 3.10+ | 免费，官方下载：<https://www.python.org/downloads/> |
| 一个浏览器 | **Chrome 或 Edge**（语音功能必须要用这两个） |
| 一个 DeepSeek API Key | 免费注册领取：<https://platform.deepseek.com>（充值一点点就够用很久） |
| 能上网 | 对话、语音、爬题库都需要联网 |

> 小提示：语音功能需要**麦克风**；用外放时如果回声大、打断不灵敏，戴上耳机更好。

---

## 二、安装 Python

### Windows

1. 打开 <https://www.python.org/downloads/>
2. 点黄色按钮 **Download Python 3.x** 下载
3. 双击安装包，**一定要勾选**底部的 `Add Python to PATH`（这步很关键，不勾会找不到 Python）
4. 一路点「下一步」完成安装

### macOS

1. 打开 <https://www.python.org/downloads/>
2. 下载 macOS 版并安装
3. 安装完成后打开「终端」（Spotlight 搜索 Terminal），输入下面命令回车，看到版本号就说明装好了：

```bash
python3 --version
```

### 验证安装

Windows 打开「命令提示符」（开始菜单搜 cmd），macOS/Linux 打开「终端」，输入：

```bash
python --version
```

能看到 `Python 3.10+` 的版本号就 OK。

---

## 三、拿到项目代码

**方式 A（推荐给小白）：直接拷贝文件夹**

把整个项目文件夹（包含 `app/`、`main.py` 所在的那一层）复制到你的电脑上，比如放桌面。

**方式 B：用 Git 拉取**

如果你会一点 Git：

```bash
git clone <项目仓库地址>
cd 智能面试
```

---

## 四、配置密钥（最关键的一步）

项目里有一个文件叫 `.env.example`（配置模板）。你要把它复制一份，改名为 `.env`，
再填上你自己的密钥。`.env` 是私密文件，**不要发给别人、不要传到网上**。

### Windows

在项目文件夹里打开文件资源管理器，找到 `.env.example`，复制一份，重命名为 `.env`。

### macOS / Linux

在终端里进入项目文件夹后运行：

```bash
cp .env.example .env
```

### 填写密钥

用记事本/文本编辑器打开 `.env`，把下面这行的 `sk-你的key` 换成你自己的真实密钥：

```ini
DEEPSEEK_API_KEY=sk-你的key
```

**Key 去哪领？** 打开 <https://platform.deepseek.com> → 注册登录 → 左侧「API Keys」→
创建 Key → 复制出来填进去。

> 可选：想让语音更自然、更稳定，可以再配一个阿里云 CosyVoice 的 Key
> （<https://bailian.console.aliyun.com/> 获取），填到 `.env` 里；这个 Key 同时用于
> **语音识别（Paraformer）** 与 CosyVoice 音色（`VOICE_TTS=cosyvoice`）。不配置的话
> 语音识别不可用，语音通话只能听不能说；TTS 仍可用默认的 edge-tts。

---

## 五、启动项目

### Windows：双击启动

双击项目文件夹里的 **`scripts/start.bat`**。它会自动：

1. 安装所需依赖（第一次会等几分钟）
2. 构建前端（首次需要 Node.js 18+，会等一会儿）
3. 启动统一服务（默认 8765 端口，被占用会自动换 8766）
4. 自动打开浏览器

看到浏览器打开 `http://localhost:8765` 就说明成功了。**启动窗口别关**，关了服务就停了。

### macOS / Linux：一键脚本

```bash
bash scripts/start.sh
```

### 手动启动（进阶）

先装依赖：

```bash
pip install -e .
```

再构建前端（首次）：

```bash
cd frontend && npm install && npm run build && cd ..
```

最后开一个终端启动统一服务：

```bash
python -m uvicorn app.main:app --host 127.0.0.1 --port 8765
```

---

## 六、怎么用（文字版）

打开浏览器访问 **http://localhost:8765**（或启动脚本提示的地址）。**首次使用先注册一个账号**（用户名 + 密码），登录后你会看到首页三张卡片：

### 1. 模拟面试 🎤

点「开始面试」，小P 会按真实流程走：

> 自我介绍 → 六阶段逐题提问（Python 基础 → 算法 → 数据库 → 网络 → 项目 → 场景设计）
> → 每题点评 + 追问 → 结束输出 0-100 评分报告

你就像面试一样，把答案打进去，小P 负责问和评。

### 2. 辅导答疑 💡

点「开始答疑」，然后直接输入问题，比如：

> Redis 缓存穿透怎么答？

小P 会给出：标准参考回答 + 加分点 + 一道变式题，还能自动检索本地题库辅助回答。

### 3. 定制面试 🎯

点「开始定制」，填你应聘的**岗位**（比如"高级 Python 后端工程师"）和**招聘信息（JD）**，
小P 会针对这个岗位生成一套专属面试题，然后逐题面你。

### 4. 题库浏览与收藏 📚

点顶部或侧边栏的「📚 题库」按钮：

- 按来源 / 难度 / 公司 / 关键词 / **标签**筛选
- 点「⭐ 收藏」收藏题目，勾选「⭐ 仅看收藏」查看收藏
- 点「加入面试」把题目加入右侧列表，可以**多选几道**，再点「🚀 开始综合面试」
  用你选的题目一起做一场面试

---

## 七、怎么用（语音通话）

1. 打开 **http://localhost:8765/voice**（或点文字版右下角的 📞 按钮）
2. 点绿色 **📞 接通** 按钮（浏览器会询问麦克风权限，点允许）
3. 小P 会**主动开口问候**，你直接说话就行
4. **想打断**：小P 说话时你直接开口，它会停下听你讲
5. 说「开始面试」自动切换成模拟面试；直接提问就是辅导答疑
6. 想结束：点红色 **⏹ 挂断**

> 语音识别由服务端阿里云 Paraformer 完成（需在 `.env` 配置 `DASHSCOPE_API_KEY`）；
> 浏览器请用 **Chrome 或 Edge**，别的浏览器可能不支持麦克风采集。

---

## 八、常见问题（小白必看）

### Q1：双击 bat 后黑窗口一闪就没了

通常是 Python 没装好或没勾 `Add Python to PATH`。重新安装 Python，勾选那一项，再试。

### Q2：网页打不开 / 一直转圈

- 确认启动脚本的黑色窗口还开着（服务在跑）
- 刷新浏览器（`Ctrl+Shift+R` 强制刷新）
- 确认地址是 `http://localhost:8765` 或脚本提示的地址

### Q3：提示"未检测到 DEEPSEEK_API_KEY"

`.env` 没建好或 Key 没填对。回到第四步检查：文件名是不是 `.env`（不是 `.env.example`），
Key 前面没有多余空格。

### Q4：文字回复正常，但语音没声音 / 语音识别没反应

- 确认用的是 Chrome 或 Edge
- 确认访问的是 **http://localhost:8765/voice** 而不是别的地址
- 浏览器地址栏左侧的麦克风权限是否允许
- 电脑音量、系统麦克风是否打开

### Q5：语音说话卡顿 / 断断续续 / 打不断

- 优先戴耳机（外放回声大时最容易出问题）
- 音量调到正常大小，别开太响
- 检查网络（语音合成需要联网）

### Q6：题库是空的

首次启动后会自动爬取题库，需要几分钟；也可以手动爬：

```bash
python -m app.crawler.run
```

### Q7：端口被占用

启动脚本会自动换端口（8765 → 8766）。想固定其他端口时，改 `.env` 里的
`APP_PORT` 换个数字。

### Q8：会不会扣很多钱？

DeepSeek 对话很便宜（一次面试大约几分钱到几毛钱）；edge-tts 语音免费。
CosyVoice 有免费额度。正常练习完全不用担心。

---

## 九、进阶小贴士

- **换音色**：在 `.env` 里改 `COSYVOICE_VOICE`（默认龙小淳甜美女声），语速 `COSYVOICE_RATE`，音调 `COSYVOICE_PITCH`
- **跑测试**：`python -m unittest discover -s tests`
- **容器部署**：装了 Docker 的话，`docker compose up -d`（需把密钥配到环境变量）
- **数据备份**：`data/questions.db` 是题库，拷走就能带到新电脑

---

## 十、安全提醒

- `.env` 里的密钥是**你的钱**，别截图发群里、别提交到 GitHub
- 这个项目已经默认把 `.env` 排除在版本控制之外，但你自己也别手动提交它

祝面试顺利，拿到心仪的 Offer！🎉
